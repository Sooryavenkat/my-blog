[build]
  publish = "public"
  command = "curl -sL https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz -o hugo.tar.gz && tar -xzf hugo.tar.gz && ./hugo --gc --minify"

[build.environment]
  HUGO_VERSION = "0.162.1"
